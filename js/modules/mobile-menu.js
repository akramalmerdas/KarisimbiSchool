/**
 * Mobile Menu Module
 */

export function initMobileMenu() {
  const navLinks = document.querySelector('.nav-links');
  const navApply = document.querySelector('.nav-apply');
  
  // In a real implementation, we would add a hamburger icon
  // For now, we'll just log that the module is ready
  console.log('Mobile menu module ready');
}
